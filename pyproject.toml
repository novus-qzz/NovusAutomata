[project]
name = "novusautomata-demo"
version = "0.1.0"
description = "Demo for NovusAutomata adaptive CI"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]

[tool.pytest.ini_options]
testpaths = ["tests"]